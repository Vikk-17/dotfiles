# i3 config file

# Windows key
set $mod Mod4

# For for window titles and also will be used by the bar unless a different font is used in the bar {} block below
font pango:Droid Sans 16

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8


# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod


# start a terminal
bindsym $mod+Return exec kitty

# kill focus window
bindsym $mod+Shift+q kill

# start rofi (a program launcher)
bindsym $mod+z exec --no-startup-id ~/.config/rofi/launchers/type-1/launcher.sh
bindsym $mod+x exec --no-startup-id powermenu_t2
